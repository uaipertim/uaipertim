export type DataSourceMode = 'demo-local' | 'firebase-development';

// Define which data source should be used globally
// As requested, the initial mode must be 'demo-local'
export const DATA_SOURCE: DataSourceMode = 'demo-local';
